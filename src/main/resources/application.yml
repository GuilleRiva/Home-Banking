server:
  port: 8080

spring:
  application:
    name: home-banking-api
  jpa:
    open-in-view: false

management:
  endpoints:
    web:
      exposure:
        include: "health,info"
  metrics:
    export:
      prometheus:
        enabled: true

